[phases.setup]
nixPkgs = [
  "python310",
  "glib",
  "libGL",
  "libglib",
  "libgcc",
  "libstdc++",
  "zlib"
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "gunicorn app:app"